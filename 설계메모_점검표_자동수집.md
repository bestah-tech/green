# 심사점검표 자동 수집 매크로 — 설계 메모

## 목표
출원상표 심사점검표 목록에서 **별표 친** 선행건(타인선출원 / 본인선출원 /
본인선등록 / 타인선등록)에 한해:
1. 점검표 팝업을 자동으로 순서대로 열고
2. 셀프캡처 6판과 같은 수집 엔진(DOM 덤프 + GetFieldTextData)으로 내용 수집
3. 전 건을 [구분선]으로 이어 클립보드로 내보내기 (뷰어에 한 번 붙여넣기)

## 전제 (이미 확인된 사실)
- 내부 PC, IE 기반 특허넷. `window.clipboardData` 사용 가능.
- 즐겨찾기 javascript: URL은 약 2,963자 초과 시 조용히 실행 거부됨.
- `window.__변수`는 같은 창에서 즐겨찾기를 여러 번 눌러도 유지됨
  (v6.7의 `W.__a` 플래그로 검증).

## 구조: 즐겨찾기 2개로 분할 (길이 제한 회피)
- **[설치]**: 수집 엔진 함수(dp, hw)와 큐 상태를 `window.__M`에 정의만 함.
- **[실행]**: 목록에서 별표 건을 스캔해 큐를 만들고, `setInterval` 루프로
  팝업 열기 → 로딩 대기 → 수집 → 닫기 → 다음 건 진행.
  완료 시 클립보드에 전체 결과 기록 + '완료 N건' 알림.
- 사용법: 목록 화면에서 [설치] 한 번 → [실행] 한 번 → 알림 뜨면 뷰어에 Ctrl+V.

## 실행 루프 상세
1. 별표 스캔: 4개 구분(타인선출원 등) 아래 행 중 별표 표시가 있는 행 수집.
   → 별표의 실체(★ 텍스트 / 아이콘 이미지 / 체크박스)는 정찰 결과로 확정.
2. 팝업 열기: 각 행의 링크 href(`javascript:함수(인자)`) 또는 onclick을
   추출해 직접 호출. `window.open` 후킹(v6.7 방식)으로 팝업 핸들 확보.
3. 대기: 팝업 `document.body` 접근 가능 + 본문 글자수 일정 이상이 될 때까지
   폴링(0.5초 간격, 최대 15초). 타임아웃 건은 '실패 목록'에 기록하고 계속.
4. 수집: 셀프캡처 6판의 dp(신형 DOM) + hw(구형/국제 GetFieldTextData) 재사용.
5. 닫기 후 다음 건. 서버 부하를 피하려고 반드시 한 번에 한 팝업만.
6. 종료: `[구분선]`으로 이어 클립보드 기록. 실패 건 번호를 알림에 표시
   → 실패 건만 기존 셀프캡처로 수동 보완.

## 리스크 / 미확정
| 항목 | 내용 | 대응 |
|---|---|---|
| 별표 표현 방식 | 텍스트/이미지/체크박스 미확정 | 정찰 북마클릿 결과로 확정 |
| 목록이 ActiveX 그리드일 가능성 | DOM에 행이 안 보이면 링크 호출 불가 | 정찰의 OBJ 목록으로 판별. ActiveX면 그리드 API 조사 단계 추가 |
| 팝업이 같은 창을 재사용 | 핸들 추적 방식 변경 필요 | window.open 후킹으로 파악 가능 |
| 팝업 로딩 속도 편차 | 국제건 등은 느릴 수 있음 | 타임아웃+실패목록으로 안전 처리 |

## 진행 상태
- [x] 정찰용 북마클릿 작성 (`정찰용_점검표목록.txt`, 2,032자)
- [ ] 정찰 결과 확보 (사용자가 목록 화면에서 실행 후 결과 전달)
- [ ] 본편 [설치]/[실행] 북마클릿 작성
- [ ] 실전 테스트 (별표 2~3건으로 소규모 → 전체)
