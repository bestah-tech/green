# TRADEMARK 진행 상황 (이어서 하기용)

마지막 업데이트: 2026-08-31

## 지금까지 한 것

- **1단계(뼈대) 완료**: MV3 확장, 팝업(모듈 런처 + 공통 설정), LLM 공통 래퍼, IndexedDB
- **2단계(모듈 1) 완료**: 출원상표 분석(표장 구성·외국어 의미·지정상품+유사군코드) + 승인 1(버전 체인)
- 내부망 반입 성공: **암호 걸린 ZIP**(암호 `tm2026!`)으로 파일전송시스템 통과 → 내부망 크롬에 로드 확인
- 코드 저장 위치: GitHub `bestah-tech/green`, 브랜치 `claude/md-instruction-program-lf3xh3`

## 내일 재개 시 첫 할 일 (LLM 신규가입 완료 후)

1. 내부망 OpenWebUI(`https://llm.moip.go.kr`) 로그인 → 설정 → 계정 → API 키 발급
2. TRADEMARK 팝업 → 공통 설정 입력:
   - API Base URL: `https://llm.moip.go.kr`
   - API 방식: `OpenWebUI (내부망)`
   - API Key: 발급받은 `sk-...`
   - 저장 → **모델 목록** 버튼으로 연결 확인 → `gemma4-26b-moe` 선택
3. 통합 대시보드 → 연결 테스트 → 출원건 생성 → 모듈 1 실제 분석 실행 테스트
4. 26B 모델이 자주 틀리는 출력 형식이 있으면 메모 → "교정지시" 기능으로 보완

## 아직 안 한 것 (다음 단계)

- **3단계 (MVP 완성)**: 모듈 5 최소판 — 조문별 문구 템플릿 + 모듈 1 결과로 의견제출통지서 초안 + HWPX 출력
  - 필요: 의견제출통지서·거절결정서 **HWPX 샘플** (받는 대로 서식 반영)
- 4단계: 모듈 2 검색식 작성 + 승인 2
- 5단계: 모듈 3 사이드패널 검색 에이전트
  - 필요: 내부망 **상표검색시스템 URL·검색결과 HTML 구조** → `config/selectors.json` 교체
- 6단계: 모듈 4 유사판단 리뷰 + 판례·심사기준 문구 자산
- 7단계: 모듈 5 완성 (근거 제한·대화형 수정)
- 8단계: 내부망 최종 반영

## 업데이트 배포 방법 (매번)

새 버전은 매번 **암호 ZIP**으로 만들어 전달 → 외부망에서 파일전송시스템에 그대로 올림 →
내부망에서 압축 풀고 → `chrome://extensions`에서 TRADEMARK 카드의 **새로고침(⟳)** 클릭.
