# 특허넷 심사점검표 자동수집 프로젝트 — 현황 정리 (인수인계용)

> 새 채팅을 열 때, 이 문서 전체를 첫 메시지로 붙여넣고
> "이 프로젝트를 이어서 하고 싶어. 현재 상태 파악했으면 알려줘"라고 하면 됩니다.
> 최종 파일 2개(북마클릿 코드, 뷰어 HTML)도 프로젝트 지식에 올려두면 좋습니다.

---

## 1. 목적

특허넷(내부망, Internet Explorer 기반) 상표 심사 시, 선등록·선출원 상표의
**심사점검표 팝업 내용을 텍스트로 수집** → 별도 **HTML 뷰어**에서 건별로
정리해 한눈에 보는 도구. 하루 15건 × 선행건 10~15건을 검토하는
반복 업무의 병목(팝업 하나하나 열어 선심 판단 읽기)을 줄이는 것이 목표.

**보안 제약**: 내부 자료는 외부 AI로 반출 불가. 그래서 "화면에 보이는
텍스트를 로컬에서 정리"하는 방식만 사용. 코드(북마클릿/HTML)는 반출 대상이
아니므로 외부에서 만들어 내부망으로 반입. 바이브코딩 보안점검 절차는
사내 매뉴얼 기준으로 통과 확인됨(읽기전용·외부통신 없음·XSS 이스케이프 완료).

---

## 2. 최종 구성 (현재 작동하는 버전)

### (A) 북마클릿 = "v6.1"  → 파일: `★v6.1_카운트수정.txt`
- 특허넷 IE 즐겨찾기 URL에 코드를 넣어 실행. 실행하면 화면 오른쪽 아래에
  작은 패널 등장: `[숫자] IN CLIP OUT CLR`
- **동작 방식**: `window.open`을 감시(가로채기)해서, 열린 팝업 창들을 추적.
  [IN] 누르면 "가장 최근에 연 팝업"의 화면 텍스트를 수집.
  ※ 이 PC는 `Shell.Application`(창 목록 조회)을 "권한 없음"으로 차단하므로,
    그 방식(v5 계열)은 쓰면 안 됨. 반드시 window.open 방식(v4/v6 계열).
- **버튼**:
  - `IN` = 팝업 화면 담기. 건수 +1.
  - `CLIP` = 클립보드 내용을 "직전 IN 건에 붙임". **건수 안 올라감**(구형용).
  - `OUT` = 담은 전체를 클립보드로 복사 → 뷰어에 붙여넣기.
  - `CLR` = 비우기.
- **알림**: READY v6=시작 / DUP=중복 / NO POPUP=팝업못찾음 /
  CLIP EMPTY=클립보드비어있음 / +CLIP=클립붙임성공 / 숫자=OUT시 복사건수.

### (B) 뷰어 = "v8"  → 파일: `심사점검표_정리뷰어.html`
- 바탕화면 등에 두고 IE로 열기. 제목 옆에 **v8 배지**가 보이면 최신본.
- v8: 2021년 무렵 신형 변형 양식 대응(파서 재작성), 판단의견 사유 표시,
  본인 선등록/동일자출원 표시, 출원번호 파이프 표기 인식.
- 북마클릿은 v6.1 그대로 써도 됨. 선택으로 v6.2(`북마클릿_v6.2.txt`,
  [OUT] 시 건 사이 [구분선] 삽입 → 건 분리 100% 정확)가 있음.
- 파서 검증: 저장소의 `test/parse_test.js` (node test/parse_test.js)
- OUT한 내용을 입력칸에 붙여넣고 "붙여넣은 내용 정리하기" 클릭.
- 출원번호(`40-XXXX-XXXXXXX`, 콜론형 `(출원번호 : ...)`도 인식) 기준으로
  건별 자동 분리. 여러 건·신형/구형 혼합 가능. 누적 모드로 하루치 쌓기 가능.
- **버튼**: 클립보드에서 가져오기 / 붙여넣은 내용 정리하기 / 전체 초기화 /
  진단용 추출(익명화) / 자동 감지(2초마다 클립보드 확인).

---

## 3. 사용 흐름 (확정)

**신형 점검표** (2016년 이후, 표장분석/식별력/유사판단 구조):
1. 선행건 팝업 열기 → `IN`  (끝. 한 번이면 됨)

**구형 점검표** (2015~16 이전, 【검색결과】【심사내용】【판단의견】 + HWP 편집기 박스):
1. 팝업 열기 → `IN`
2. **권장(통복사)**: 커서를 기재 박스 "바깥"(【검색결과】 제목 옆)에 찍고
   Ctrl+A → Ctrl+C → `CLIP` 한 번. (또는 편집기 메뉴 편집(E)→모두 선택)
   → 뷰어 v8이 【검색결과】【심사내용】【의견요약】【판단의견】 표제로
   자동 분리하므로 한 번이면 충분.
   통복사가 안 먹히면 기존처럼 박스별로: 박스 클릭 → Ctrl+A → Ctrl+C →
   `CLIP` (박스마다 반복, 건수 안 올라감)
   ※ 순서 필수: IN 먼저, CLIP 나중 (CLIP은 직전 IN 건에 붙으므로)
3. **실험**: 북마클릿 v6.3(`북마클릿_v6.3_실험.txt`)은 [IN] 때 HWP
   편집기 본문을 자동 추출 시도(성공 시 CLIP 자체가 불필요).
   차단되면 조용히 무시되므로 설치해서 한 번 시험해볼 가치 있음.

**공통 마무리**: 한 심사건 끝 → `OUT` → 뷰어에 붙여넣기 → 정리 → `CLR`

---

## 4. 뷰어가 보여주는 것

**신형 카드**: 표장명 + 출원번호 + 식별력 유무 + 식별력 사유(원문) +
표장분석 + 타인 선등록/선출원 표(심사관 의견 기재 행은 강조, ★유사는 빨강·
☆는 노랑) + 유사판단 종합의견 + 사용실태 + 최종판단(등록/거절 도장).
요약표에서 건별 한눈 조망 + "의견 기재된 선행건만 보기" 필터.

**구형 카드**: 지정상품 + (검색결과 조각에서 추출한) 타인 선등록/선출원 표 +
CLIP한 조각들을 담은 순서대로 원문 그대로 표시(검색결과/심사내용/판단의견).

---

## 5. 그동안 해결한 핵심 이슈 (같은 실수 반복 방지용)

1. **팝업이 메뉴바 없이 떠서 즐겨찾기 클릭 불가** → IE에서 Ctrl+I로 즐겨찾기
   목록 열기. 또는 주소창 실행.
2. **코드가 길어 즐겨찾기 URL에서 잘림** → 코드를 최대한 짧게(약 2,900자 내).
   메모장에서 [서식]→[자동 줄바꿈] 해제 후 한 줄로 복사해 넣어야 안 잘림.
   붙여넣은 뒤 속성에서 맨 끝이 `})();`로 끝나는지 확인.
3. **로컬 .js 파일 로드 방식 = "사용 권한 없음"으로 차단됨** → 이 PC는 IE가
   로컬 파일 읽기를 막음. 파일 로드/HTML 런처의 Shell 접근도 같은 이유로 불가.
   그래서 **즐겨찾기에 코드 직접 넣기**가 정답.
4. **팝업이 아예 안 열림** → `window.open` 가로채기에서 `apply` 호출이 IE
   내장함수에서 실패. → 원본 open 호출을 3중 폴백으로 감싸 해결(v6).
5. **v5의 Shell.Application 방식 = 무반응** → 이 PC가 창목록 조회를 차단.
   v4/v6의 window.open 감시로 복귀해야 함.
6. **신형이 구형으로 오인식** → 구형 감지 조건을 넓히다 '제33조' 문구 있는
   신형까지 구형 처리. → **신형 고유 표지(표장분석/식별력 유무판단)가 있으면
   무조건 신형**으로 확정(우선순위)해서 해결.
7. **선등록 여러 행이 1행 의견칸에 뭉침** → 기재 블록 끝 경계를, 출원건 창
   덤프의 ` | [선택: ]`·`[○]`·다음 행 시작까지 인식하도록 확장해 해결.
8. **심사관 기재문이 잘림**(예: 식별력 사유에 '유사판단' 소제목이 있으면 거기서
   끊김) → 기재 블록을 먼저 토큰으로 보호한 뒤 구획 분리, 표시 때 원문 복원.
9. **별표 오인식**(☆인데 빨강) → 선택박스에서 "선택된 값만" 수집하도록 수정.
   뷰어도 [선택:] 값 우선 판정 + 옵션 전체 긁힌 덤프 오탐 방지.
10. **CLIP 카운트 중복**(구형이 건당 2 올라감) → CLIP은 카운트 없이 직전 건에 붙임.
11. **스크롤 먹통** → 가로 넘침이 원인. overflow-x:hidden + word-break로 해결.
12. **구형 편집기가 HWP 특수 편집기** → 브라우저가 내부를 못 읽음. 박스별
    Ctrl+A/C → CLIP이 유일한 경로(박스가 여러 개라 박스마다 반복).
13. **2021년 무렵 신형 변형 양식이 통째로 파싱 실패** (뷰어 v8에서 해결) →
    신형이라도 연식에 따라 표기가 다름:
    `유사판단 [34조, 35조 (구 7조, 8조)]`처럼 표제에 꼬리가 붙고,
    `식별력 유무판단` 라벨 없이 라디오만 나오고, 등록번호가 7~9자리이고,
    `1차 착수심사결과`(붙여쓰기)·`기타34조(구 7조)판단` 등 띄어쓰기가 다름.
    → 표제를 "정확한 한 줄"로 찾지 말고 위치 기준점(anchor) + 관대한
    정규식으로 구획을 자르는 방식으로 파서 전면 재작성.
14. **`[입력내용]` 블록이 다음 구획을 삼킴** → 경계를 일반화:
    파이프로 시작하는 줄, `[○][●]`·`[선택:]`·`[입력:]` 표식이 있는 줄,
    `=====`, 다음 선행건 행 시작. (기재 원문은 textarea 값이라 이런 표식이
    있을 수 없음 → 안전)
15. **출원번호가 `출원번호 | 40-...`처럼 파이프 구분으로 나오면 미검출** →
    콜론/파이프 모두 허용(APPRE). 건 분리도 같은 규칙 + 같은 번호 조각
    재결합 + 번호 없는 머리 조각(좌측 지정상품 표)은 다음 조각에 병합.
16. **판단의견 사유·의견요약·본인 선등록/동일자출원을 표시 안 함** →
    v8에서 카드에 추가 (판단의견 사유는 펼침 상태로 최상단).
17. **국제등록상표는 [IN]이 심사이력정보를 담아옴** → 국제건은
    [심사이력정보 팝업] → 클릭 → [심사점검표 팝업] 2단계로 열리는데,
    감시가 메인 창에만 걸려 있어 2차 팝업(점검표)이 추적 안 됐음.
    → v6.4: 추적된 팝업 안에서 열리는 2차 팝업까지 재귀 감시. (실패)
    → v6.5: 감시 표식을 문서에 저장(문서 이동 시 재감시) + 그래도
    안 잡히는 창은 **셀프캡처** 북마클릿(점검표 창 안에서 Ctrl+I로
    직접 실행 → [팝업캡처] 표식과 함께 클립보드 복사)으로 우회.
    v6.5의 [CLIP]은 [팝업캡처] 표식을 보면 새 건으로 담음(건수 +1).
    뷰어 v8은 하이픈 없는 13자리 출원번호(4020140063931)도 정규화 인식.

---

## 6. 다음에 문제 생기면 쓸 진단법

- **뷰어가 특정 건을 잘못 정리** → 그 건을 붙여넣고 뷰어의
  **[진단용 추출(익명화)]** 버튼 클릭 → 이름·표장이 O로 마스킹된 텍스트가
  나옴 → 그걸 복사해 새 채팅의 Claude에게 주면 구조 진단 가능(민감정보 보호).
- **뷰어 버전 확인**: 제목 옆 배지가 v7인지. 아니면 캐시된 옛 파일이므로
  Ctrl+F5로 새로고침 또는 파일 재저장.
- **북마클릿이 무반응** → 즐겨찾기 URL 맨 앞 `javascript:` 있는지,
  맨 끝 `})();`로 끝나는지(잘림 여부) 확인.

---

## 7. 아직 안 한 것 / 향후

- 로컬 LLM(Ollama + Llama/EXAONE) 연계로 수집 내용 요약 — 파이프라인 안정화
  후 착수 예정. 프롬프트 설계는 뷰어 출력이 안정된 뒤에.
- 실전에서 신형/구형 다양한 변형을 더 돌려보며 파서 예외 보강 필요.

---

## 8. 새 채팅에 넘길 것 체크리스트

- [ ] 이 문서 전체 (첫 메시지로 붙여넣기)
- [ ] `심사점검표_정리뷰어.html` (현재 v7, 최종 뷰어) — 프로젝트에 업로드
- [ ] `★v6.1_카운트수정.txt` (현재 북마클릿 코드) — 프로젝트에 업로드
- [ ] (선택) 문제 재현이 필요하면, 익명화 추출한 실제 덤프 샘플
